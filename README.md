# fedora_ansible

Prereq:

````
sudo dnf install ansible
ansible-playbook restore.yml --ask-become-pass
````

1. Run the  `setup_script.sh` to output the setup for the ansible playbook to use as parsed values